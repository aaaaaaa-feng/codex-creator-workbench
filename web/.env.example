# Copy this file to .env.local, then replace the example values.
NEXT_PUBLIC_CREATOR_NAME=你的名字
NEXT_PUBLIC_CREATOR_MARK=创
NEXT_PUBLIC_STUDIO_LABEL=CREATOR STUDIO

# The bridge reads these server-side values.
CREATOR_NAME=你的名字
COVER_SKILL_NAME=artifact-template-creator

# Optional: point the workbench at a separate content directory.
# WORKBENCH_CONTENT_ROOT=/absolute/path/to/your/content-workspace
